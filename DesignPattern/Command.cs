using System;
using System.Collections.Generic;
using System.Text;

namespace DesignPattern
{
    class Command
    {
        /* 命令模式
           類似轉接器，把物件包起來，用某個一致的方法去執行物件的特殊指令，是一種依賴反轉的做法
           命令接收者(Invoke) 可以用佇列的方式依序完成任務，也可以延宕、取消之類的。
         */
    }
}
